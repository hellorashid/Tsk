# Shared Package Area

This directory is intentionally light for now.

When mobile and desktop work begins, this is the default place to extract:

- shared TypeScript types
- shared utilities
- shared design tokens
- cross-platform state or domain helpers
